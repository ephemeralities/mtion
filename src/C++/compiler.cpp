#include <string>
#include <fstream>
#include <vector>

void open_file(std::string src){
    std::ifstream file(src);
    
    for(int i = 0; i < file.eof; i++){
        
    }
}

std::string token_type(std::string token){

}